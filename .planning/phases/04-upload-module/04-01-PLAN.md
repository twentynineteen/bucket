---
phase: 04-upload-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/Upload/api.ts
  - src/features/Upload/types.ts
  - src/features/Upload/index.ts
  - src/features/Upload/hooks/useFileUpload.ts
  - src/features/Upload/hooks/useUploadEvents.ts
  - src/features/Upload/hooks/useImageRefresh.ts
  - src/features/Upload/hooks/useSproutVideoApi.ts
  - src/features/Upload/hooks/useSproutVideoPlayer.ts
  - src/features/Upload/hooks/useSproutVideoProcessor.ts
  - src/features/Upload/hooks/usePosterframeCanvas.ts
  - src/features/Upload/hooks/usePosterframeAutoRedraw.ts
  - src/features/Upload/hooks/useFileSelection.ts
  - src/features/Upload/hooks/useZoomPan.ts
  - src/features/Upload/components/UploadSprout.tsx
  - src/features/Upload/components/UploadSprout.test.tsx
  - src/features/Upload/components/Posterframe.tsx
  - src/features/Upload/components/UploadOtter.tsx
  - src/features/Upload/components/FolderTreeSprout.tsx
  - src/features/Upload/internal/parseSproutVideoUrl.ts
  - src/features/Upload/internal/loadFont.ts
  - src/features/Upload/__contracts__/upload.contract.test.ts
  - src/features/Trello/hooks/useVideoLinksManager.ts
  - src/features/Trello/__contracts__/trello.contract.test.ts
autonomous: true
requirements: [UPLD-01, UPLD-02, UPLD-03]

must_haves:
  truths:
    - "Importing from @features/Upload barrel provides all 4 components (UploadSprout, Posterframe, UploadOtter, FolderTreeSprout) and all 9 hooks (useSproutVideoPlayer dropped as dead code)"
    - "No Upload component or hook calls invoke(), listen(), openDialog(), readFile/writeFile, fontDir(), or exists() directly -- all I/O goes through api.ts"
    - "Internal utilities (parseSproutVideoUrl, loadFont) are NOT exported from barrel"
    - "Trello module's useVideoLinksManager imports Upload hooks from @features/Upload barrel, not @hooks/"
    - "Contract tests validate barrel shape (exact export list) and key behavioral contracts"
    - "useSproutVideoPlayer dead code removed (per locked decision: drop entirely, zero consumers)"
  artifacts:
    - path: "src/features/Upload/index.ts"
      provides: "Upload barrel with named re-exports of 4 components + 9 hooks"
      min_lines: 15
    - path: "src/features/Upload/api.ts"
      provides: "Upload I/O boundary wrapping invoke + listen + dialog + fs plugins"
      min_lines: 40
    - path: "src/features/Upload/types.ts"
      provides: "Upload module types, re-exporting relevant shared types"
      min_lines: 5
    - path: "src/features/Upload/__contracts__/upload.contract.test.ts"
      provides: "Upload contract tests (shape + behavioral)"
      min_lines: 40
  key_links:
    - from: "src/features/Upload/hooks/*"
      to: "src/features/Upload/api.ts"
      via: "import from api layer for all I/O"
      pattern: "import.*from.*[./]+api"
    - from: "src/features/Upload/api.ts"
      to: "@tauri-apps/api/core"
      via: "invoke('upload_video'), invoke('get_folders'), etc."
      pattern: "invoke.*upload_video|invoke.*get_folders"
    - from: "src/features/Upload/api.ts"
      to: "@tauri-apps/api/event"
      via: "listen('upload_progress'), listen('upload_complete'), listen('upload_error')"
      pattern: "listen.*upload_"
    - from: "src/features/Trello/hooks/useVideoLinksManager.ts"
      to: "@features/Upload"
      via: "barrel import for useFileUpload, useUploadEvents, useSproutVideoApi, useSproutVideoProcessor"
      pattern: "from '@features/Upload'"
---

<objective>
Create Upload deep feature module with barrel exports, API layer, and contract tests. Migrate all Sprout Video, Posterframe, and Otter files atomically.

Purpose: Upload is a mid-tier feature module with cross-module dependencies (Trello imports 4 Upload hooks). Migrating it into a self-contained module with a single api.ts I/O boundary enables Trello, Baker, and the app shell to import cleanly from `@features/Upload` without reaching into internals.
Output: src/features/Upload/ directory with barrel exports, api.ts wrapping all I/O (invoke, listen, dialog, fs plugins), contract tests, and all consumer imports updated. Dead code (useSproutVideoPlayer) dropped.
</objective>

<execution_context>
@/Users/danielmills/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielmills/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-upload-module/04-CONTEXT.md
@.planning/phases/04-upload-module/04-RESEARCH.md

@src/hooks/useFileUpload.ts
@src/hooks/useUploadEvents.ts
@src/hooks/useImageRefresh.ts
@src/hooks/useSproutVideoApi.ts
@src/hooks/useSproutVideoPlayer.ts
@src/hooks/useSproutVideoProcessor.ts
@src/hooks/usePosterframeCanvas.ts
@src/hooks/usePosterframeAutoRedraw.ts
@src/hooks/useFileSelection.ts
@src/hooks/useZoomPan.ts
@src/pages/UploadSprout.tsx
@src/pages/UploadSprout.test.tsx
@src/pages/Posterframe.tsx
@src/pages/UploadOtter.tsx
@src/pages/FolderTreeSprout.tsx
@src/utils/parseSproutVideoUrl.ts
@src/utils/loadFont.ts
@src/features/Trello/hooks/useVideoLinksManager.ts
@src/features/Trello/__contracts__/trello.contract.test.ts

<interfaces>
<!-- Shared infrastructure available for import -->
From src/shared/hooks/index.ts:
  exports: useBreadcrumb, useReducedMotion, useFuzzySearch, useUsername, useApiKeys, useSproutVideoApiKey, useTrelloApiKeys

From src/shared/store/index.ts:
  exports: useBreadcrumbStore, useAppStore, appStore

From src/shared/types/index.ts:
  exports: SproutUploadResponse, GetFoldersResponse, SproutVideoDetails, and other media/domain types

From src/shared/utils/index.ts:
  exports: logger, createNamespacedLogger, loadApiKeys, storage

<!-- Existing feature module pattern to follow -->
From src/features/Trello/index.ts:
  Pattern: named re-exports of components + hooks, no wildcards

From src/features/Trello/api.ts:
  Pattern: wraps invoke(), fetch(), readTextFile/writeTextFile as single I/O boundary

From src/features/Trello/__contracts__/trello.contract.test.ts:
  Pattern: vi.mock('../api'), barrel shape tests, renderHook behavioral tests

<!-- Cross-module consumer that needs import update -->
From src/features/Trello/hooks/useVideoLinksManager.ts:
  Currently imports: useFileUpload, useSproutVideoApi, useSproutVideoProcessor, useUploadEvents from @hooks/
  Must change to: import from @features/Upload barrel
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Upload api.ts and types.ts, move all files into module, update consumers</name>
  <files>
    src/features/Upload/api.ts,
    src/features/Upload/types.ts,
    src/features/Upload/index.ts,
    src/features/Upload/hooks/* (9 hook files -- useSproutVideoPlayer DROPPED),
    src/features/Upload/components/* (4 component files + 1 test file),
    src/features/Upload/internal/* (2 internal files),
    src/features/Trello/hooks/useVideoLinksManager.ts,
    src/features/Trello/__contracts__/trello.contract.test.ts,
    src/components/Baker/VideoLinksManager.test.tsx (mock path update)
  </files>
  <action>
    **Step 1: Create api.ts (I/O boundary -- do this FIRST as it defines the mockable surface)**

    Create `src/features/Upload/api.ts` wrapping ALL external calls. Follow the exact pattern from RESEARCH.md code example:

    a) **Tauri command wrappers:**
       - `uploadVideo(filePath, apiKey, folderId)` wrapping `invoke('upload_video', ...)`
       - `getFolders(apiKey, parentId)` wrapping `invoke('get_folders', ...)`
       - `fetchSproutVideoDetails(videoId, apiKey)` wrapping `invoke('fetch_sprout_video_details', ...)`
       - `openFolder(path)` wrapping `invoke('open_folder', ...)`

    b) **Tauri event listener wrappers:**
       - `listenUploadProgress(callback)` wrapping `listen('upload_progress', ...)`
       - `listenUploadComplete(callback)` wrapping `listen('upload_complete', ...)`
       - `listenUploadError(callback)` wrapping `listen('upload_error', ...)`

    c) **Dialog wrappers:**
       - `openFileDialog(options)` wrapping `open()` from plugin-dialog
       - `openFolderDialog()` wrapping `open({ directory: true })`

    d) **File system wrappers:**
       - `saveFile(path, data)` wrapping `writeFile()`
       - `readFileAsBytes(path)` wrapping `readFile()`
       - `listDirectory(folderPath)` wrapping `readDir()` -- filter for image files, sort alphabetically
       - `getFontDir()` wrapping `fontDir()`
       - `fileExists(path)` wrapping `exists()`

    api.ts is a strict leaf -- imports ONLY from @tauri-apps packages and shared types. No imports from hooks or components.

    **Step 2: Create types.ts**

    Create `src/features/Upload/types.ts` that re-exports relevant shared types (SproutUploadResponse, GetFoldersResponse, SproutVideoDetails) from `@shared/types` so consumers can import from `@features/Upload`. Add any Upload-specific types found in the hook/component files during migration.

    **Step 3: Move 9 hook files (DROP useSproutVideoPlayer)**

    Per locked decision: "Drop useSproutVideoPlayer entirely -- zero consumers, broken type import from missing @/types/transcript. Dead code, recoverable from git history."

    Verify useSproutVideoPlayer has zero consumers: `grep -r "useSproutVideoPlayer" src/ --include="*.ts" --include="*.tsx"`. If zero (excluding its own definition), do NOT move it. Delete the old file in Step 7.

    Copy remaining 9 hooks from `src/hooks/` to `src/features/Upload/hooks/`:
    - useFileUpload, useUploadEvents, useImageRefresh, useSproutVideoApi
    - useSproutVideoProcessor, usePosterframeCanvas, usePosterframeAutoRedraw
    - useFileSelection, useZoomPan

    For each moved hook:
    - Replace ALL direct `invoke()`, `listen()`, `open()` (dialog), `readFile/writeFile/readDir`, `fontDir()`, `exists()` calls with imports from `../api` (or `../../api` as needed)
    - Update internal imports to reference module-local paths (../api, ../types, ./otherHook)
    - Update `parseSproutVideoUrl` import to `../internal/parseSproutVideoUrl`
    - Update `loadFont` import to `../internal/loadFont`
    - Remove `// Target: @features/Upload` tag
    - Keep `@shared/*` imports unchanged (they are already correct)

    **loadFont.ts routing per locked decision:** loadFont calls api.getFontDir(), api.fileExists(), api.readFileAsBytes() instead of importing Tauri plugins directly. Update loadFont.ts to import from `../api` for all Tauri operations.

    **useFileUpload alert() fix per locked decision:** "Fix alert() -> toast in useFileUpload -- replace legacy alert() calls with sonner toast while moving the file." Import `toast` from `sonner` and replace `alert()` calls with `toast.error()`.

    **Step 4: Move 4 component files + 1 test file**

    Copy to `src/features/Upload/components/`:
    - `src/pages/UploadSprout.tsx` -> `components/UploadSprout.tsx`
    - `src/pages/UploadSprout.test.tsx` -> `components/UploadSprout.test.tsx`
    - `src/pages/Posterframe.tsx` -> `components/Posterframe.tsx`
    - `src/pages/UploadOtter.tsx` -> `components/UploadOtter.tsx`
    - `src/pages/FolderTreeSprout.tsx` -> `components/FolderTreeSprout.tsx`

    Update all internal imports in each component:
    - Hook imports from `@hooks/*` -> relative `../hooks/*`
    - `invoke()` and Tauri plugin calls in Posterframe.tsx and FolderTreeSprout.tsx -> import from `../api`
    - Update UploadSprout.test.tsx mock paths from `@hooks/` to module-relative paths

    **Posterframe.tsx deferred imports per locked decision:** Posterframe.tsx imports useBackgroundFolder and useAutoFileSelection from @hooks/ -- these are tagged `// Target: @features/BuildProject`. Leave these imports as `@hooks/` for now. Add a comment: `// TODO(Phase 8): Move to @features/BuildProject`

    **Step 5: Move 2 internal files**

    Create `src/features/Upload/internal/`:
    - `src/utils/parseSproutVideoUrl.ts` -> `internal/parseSproutVideoUrl.ts` (per locked decision: not exported from barrel)
    - `src/utils/loadFont.ts` -> `internal/loadFont.ts` (per locked decision: routes through api.ts for Tauri operations)

    **Step 6: Create barrel (named re-exports only)**

    Create `src/features/Upload/index.ts` exporting:
    - **Components:** UploadSprout, Posterframe, UploadOtter, FolderTreeSprout
    - **Hooks:** useFileUpload, useUploadEvents, useImageRefresh, useSproutVideoApi, useSproutVideoProcessor, usePosterframeCanvas, usePosterframeAutoRedraw, useFileSelection, useZoomPan
    - **Types:** re-export relevant types from ./types

    Per locked decision: "Export all hooks from barrel" and "Export all components including UploadOtter stub."
    Do NOT export parseSproutVideoUrl or loadFont (internal/).
    Do NOT export useSproutVideoPlayer (dropped).

    **Step 7: Update cross-module consumers (atomic)**

    a) **Trello module -- useVideoLinksManager.ts:**
       Change imports from `@hooks/useFileUpload`, `@hooks/useSproutVideoApi`, `@hooks/useSproutVideoProcessor`, `@hooks/useUploadEvents` to a single import from `@features/Upload` barrel.

    b) **Trello contract test -- trello.contract.test.ts:**
       Update vi.mock paths from `@hooks/useFileUpload` etc. to `@features/Upload`.

    c) **Baker test -- src/components/Baker/VideoLinksManager.test.tsx:**
       Update vi.mock paths from `@hooks/useFileUpload`, `@hooks/useUploadEvents` to `@features/Upload`.

    d) **App router/sidebar:**
       Update imports from `@pages/UploadSprout`, `@pages/Posterframe`, `@pages/UploadOtter` to `@features/Upload` barrel.

    e) **Any other consumers** found by grepping for the old import paths.

    **Step 8: Delete old files**

    After confirming all imports resolve with `bun run build`:
    - Delete 10 hook files from `src/hooks/` (including useSproutVideoPlayer)
    - Delete `src/pages/UploadSprout.tsx`, `src/pages/UploadSprout.test.tsx`
    - Delete `src/pages/Posterframe.tsx`
    - Delete `src/pages/UploadOtter.tsx`
    - Delete `src/pages/FolderTreeSprout.tsx`
    - Delete `src/utils/parseSproutVideoUrl.ts`
    - Delete `src/utils/loadFont.ts`
  </action>
  <verify>
    <automated>cd /Users/danielmills/Documents/CODE/bucket && bun run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    Upload module exists at src/features/Upload/ with api.ts wrapping all I/O (4 invoke commands, 3 event listeners, 2 dialog wrappers, 5 fs wrappers). All 9 hooks, 4 components, and 2 internal utils moved. useSproutVideoPlayer dropped (dead code). useFileUpload alert() calls replaced with sonner toast. Trello useVideoLinksManager imports from @features/Upload. All old source files deleted. Build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write contract tests for Upload module</name>
  <files>
    src/features/Upload/__contracts__/upload.contract.test.ts
  </files>
  <action>
    Create `src/features/Upload/__contracts__/upload.contract.test.ts` following the Trello contract test pattern:

    1. **Mock the api.ts layer at module level** (single mock point):
       ```
       vi.mock('../api', () => ({
         uploadVideo: vi.fn().mockResolvedValue(undefined),
         getFolders: vi.fn().mockResolvedValue({ folders: [] }),
         fetchSproutVideoDetails: vi.fn().mockResolvedValue({}),
         openFolder: vi.fn().mockResolvedValue(undefined),
         listenUploadProgress: vi.fn().mockResolvedValue(() => {}),
         listenUploadComplete: vi.fn().mockResolvedValue(() => {}),
         listenUploadError: vi.fn().mockResolvedValue(() => {}),
         openFileDialog: vi.fn().mockResolvedValue(null),
         openFolderDialog: vi.fn().mockResolvedValue(null),
         saveFile: vi.fn().mockResolvedValue(undefined),
         readFileAsBytes: vi.fn().mockResolvedValue(new Uint8Array()),
         listDirectory: vi.fn().mockResolvedValue([]),
         getFontDir: vi.fn().mockResolvedValue('/fonts'),
         fileExists: vi.fn().mockResolvedValue(false),
       }))
       ```
       Also mock @shared/* dependencies as needed (store, lib, hooks, etc.) following Trello contract test mock patterns.

    2. **Shape tests** -- Import `* as uploadBarrel from '../index'` and verify:
       - Each expected component export exists and is a function (UploadSprout, Posterframe, UploadOtter, FolderTreeSprout)
       - Each expected hook export exists and is a function (9 hooks -- NOT useSproutVideoPlayer)
       - Exact export count matches (catch unexpected leaks)
       - Internal files (parseSproutVideoUrl, loadFont) are NOT in barrel exports
       - useSproutVideoPlayer is NOT in barrel exports (dropped dead code)

    3. **Behavioral tests** (key hooks with renderHook):
       - `useFileUpload`: returns expected shape (upload function, progress state, status)
       - `useUploadEvents`: returns expected shape (progress, status)
       - `useSproutVideoProcessor`: returns expected shape (processing function, result)
       - At least 2-3 hooks tested behaviorally to validate the api.ts mock works correctly

    4. **Verify internal isolation:**
       - Test that importing from barrel does NOT expose internal utility functions

    Run contract tests, then full test suite.
  </action>
  <verify>
    <automated>cd /Users/danielmills/Documents/CODE/bucket && bun run test -- --run src/features/Upload/__contracts__/upload.contract.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    Upload contract test validates barrel shape (4 components + 9 hooks exported, no internal leaks, no useSproutVideoPlayer) and behavioral (at least 2-3 hooks verified via renderHook with mocked api.ts). Test file passes. Full test suite also passes (`bun run test -- --run`).
  </done>
</task>

</tasks>

<verification>
1. `bun run build` succeeds (TypeScript compilation + Vite build)
2. `bun run test -- --run` full test suite passes
3. `bun run test -- --run src/features/Upload/__contracts__/upload.contract.test.ts` contract tests pass
4. `grep -r "@hooks/useFileUpload\|@hooks/useUploadEvents\|@hooks/useSproutVideoApi\|@hooks/useSproutVideoProcessor\|@hooks/useImageRefresh\|@hooks/usePosterframeCanvas\|@hooks/usePosterframeAutoRedraw\|@hooks/useFileSelection\|@hooks/useZoomPan\|@hooks/useSproutVideoPlayer" src/ --include="*.ts" --include="*.tsx"` returns zero results (no old hook imports remain)
5. `grep -r "@pages/UploadSprout\|@pages/Posterframe\|@pages/UploadOtter\|@pages/FolderTreeSprout" src/ --include="*.ts" --include="*.tsx"` returns zero results (no old page imports remain)
6. `grep -r "invoke(" src/features/Upload/ --include="*.ts" --include="*.tsx" | grep -v "api.ts"` returns zero results (no direct invoke outside api.ts)
7. `grep -r "from '@tauri-apps" src/features/Upload/ --include="*.ts" --include="*.tsx" | grep -v "api.ts"` returns zero results (no direct Tauri imports outside api.ts)
8. `grep -r "useSproutVideoPlayer" src/features/Upload/ --include="*.ts" --include="*.tsx"` returns zero results (dead code dropped)
9. `grep -r "alert(" src/features/Upload/hooks/useFileUpload.ts` returns zero results (replaced with toast)
</verification>

<success_criteria>
- Upload barrel at @features/Upload exports 4 components + 9 hooks (per locked decision: all hooks public, all components including Otter stub)
- api.ts wraps all I/O: invoke (4 commands), listen (3 events), dialog (2 wrappers), fs (5 wrappers)
- useSproutVideoPlayer dropped entirely (per locked decision: dead code)
- parseSproutVideoUrl and loadFont in internal/ not exported from barrel (per locked decision)
- loadFont routes through api.ts for Tauri operations (per locked decision)
- useFileUpload alert() replaced with sonner toast (per locked decision)
- Trello useVideoLinksManager imports from @features/Upload barrel (per locked decision: atomic update)
- Baker VideoLinksManager.test.tsx mocks updated (per locked decision: atomic update)
- App router imports updated from @pages/ to @features/Upload (per locked decision: atomic update)
- Posterframe.tsx keeps @hooks/ imports for BuildProject-targeted hooks with TODO comment (per locked decision: deferred to Phase 8)
- Contract tests pass with shape + behavioral validation
- All old source files deleted, all consumer imports updated
- Full build and test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/04-upload-module/04-01-SUMMARY.md`
</output>
