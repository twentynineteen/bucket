---
phase: 10-api-bypass-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/features/Auth/__contracts__/auth.contract.test.ts
  - src/features/Premiere/__contracts__/premiere.contract.test.ts
  - src/features/Upload/__contracts__/upload.contract.test.ts
  - src/features/Settings/__contracts__/settings.contract.test.ts
  - src/features/AITools/__contracts__/aitools.contract.test.ts
  - .planning/phases/07-baker-module/07-01-SUMMARY.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - BAKR-01
  - BAKR-02
  - BAKR-03

must_haves:
  truths:
    - "All 8 feature modules have comprehensive no-bypass contract tests"
    - "BAKR-01/02/03 are marked Complete in REQUIREMENTS.md"
    - "SUMMARY 07-01 lists BAKR-01, BAKR-02, BAKR-03 in requirements_completed"
  artifacts:
    - path: "src/features/Auth/__contracts__/auth.contract.test.ts"
      provides: "No-bypass test section"
      contains: "No Direct Plugin Imports"
    - path: "src/features/Premiere/__contracts__/premiere.contract.test.ts"
      provides: "No-bypass test section"
      contains: "No Direct Plugin Imports"
    - path: "src/features/Upload/__contracts__/upload.contract.test.ts"
      provides: "No-bypass test section"
      contains: "No Direct Plugin Imports"
    - path: "src/features/Settings/__contracts__/settings.contract.test.ts"
      provides: "No-bypass test section"
      contains: "No Direct Plugin Imports"
    - path: "src/features/AITools/__contracts__/aitools.contract.test.ts"
      provides: "No-bypass test section"
      contains: "No Direct Plugin Imports"
    - path: ".planning/REQUIREMENTS.md"
      provides: "BAKR-01/02/03 marked Complete"
      contains: "[x] **BAKR-01**"
  key_links:
    - from: "All 5 contract test files"
      to: "Their respective feature module directories"
      via: "fs.readdirSync recursive scan excluding api.ts"
      pattern: "getFilesRecursive"
---

<objective>
Add comprehensive no-bypass contract tests to the remaining 5 feature modules (Auth, Premiere, Upload, Settings, AITools) and complete Baker requirement bookkeeping.

Purpose: Ensure structural enforcement is uniform across all 8 feature modules, and correct the bookkeeping gap where BAKR requirements were not marked complete despite the work being done.
Output: All 8 modules have no-bypass tests, BAKR-01/02/03 marked Complete in REQUIREMENTS.md and SUMMARY 07-01.
</objective>

<execution_context>
@/Users/danielmills/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielmills/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-api-bypass-fixes/10-CONTEXT.md
@.planning/phases/10-api-bypass-fixes/10-01-SUMMARY.md

<interfaces>
<!-- No-bypass pattern to replicate (identical across all 5 modules) -->
From src/features/BuildProject/__contracts__/buildproject.contract.test.ts:
```typescript
import fs from 'node:fs'
import path from 'node:path'

function getFilesRecursive(dir: string, extensions: string[]): string[] {
  const files: string[] = []
  const entries = fs.readdirSync(dir, { withFileTypes: true })
  for (const entry of entries) {
    const fullPath = path.join(dir, entry.name)
    if (entry.isDirectory()) {
      if (entry.name === '__contracts__' || entry.name === 'node_modules') continue
      files.push(...getFilesRecursive(fullPath, extensions))
    } else if (extensions.some((ext) => entry.name.endsWith(ext))) {
      files.push(fullPath)
    }
  }
  return files
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add no-bypass contract tests to Auth, Premiere, Upload, Settings, and AITools</name>
  <files>
    src/features/Auth/__contracts__/auth.contract.test.ts
    src/features/Premiere/__contracts__/premiere.contract.test.ts
    src/features/Upload/__contracts__/upload.contract.test.ts
    src/features/Settings/__contracts__/settings.contract.test.ts
    src/features/AITools/__contracts__/aitools.contract.test.ts
  </files>
  <action>
  For each of the 5 contract test files, add a no-bypass test section. The pattern is identical for all -- only the module name changes.

  1. Add `import fs from 'node:fs'` and `import path from 'node:path'` at the top of each file (if not already present)

  2. Add a new describe block at the end of each file (before any closing braces):

  ```typescript
  // --- No-Bypass Tests ---

  describe('{ModuleName} Module - No Direct Plugin Imports', () => {
    const projectRoot = path.resolve(__dirname, '../../../../')
    const modulePath = path.resolve(projectRoot, 'src/features/{ModuleName}')

    function getFilesRecursive(dir: string, extensions: string[]): string[] {
      const files: string[] = []
      const entries = fs.readdirSync(dir, { withFileTypes: true })
      for (const entry of entries) {
        const fullPath = path.join(dir, entry.name)
        if (entry.isDirectory()) {
          if (entry.name === '__contracts__' || entry.name === 'node_modules') continue
          files.push(...getFilesRecursive(fullPath, extensions))
        } else if (extensions.some((ext) => entry.name.endsWith(ext))) {
          files.push(fullPath)
        }
      }
      return files
    }

    it('all non-api.ts files have zero direct @tauri-apps imports', () => {
      const allFiles = getFilesRecursive(modulePath, ['.ts', '.tsx'])
      const nonApiFiles = allFiles.filter((f) => !f.endsWith('/api.ts'))
      for (const file of nonApiFiles) {
        const content = fs.readFileSync(file, 'utf-8')
        const lines = content.split('\n')
        const tauriImports = lines.filter((line) => line.includes("from '@tauri-apps"))
        expect(tauriImports).toEqual([])
      }
    })
  })
  ```

  Replace `{ModuleName}` with: Auth, Premiere, Upload, Settings, AITools respectively.

  The helper function is inlined in each file (not shared) per Claude's discretion decision -- keeps contract tests self-contained.

  **Important:** If any of the 5 modules currently have direct @tauri-apps imports in non-api.ts files that would cause the new test to fail, fix those bypasses BEFORE adding the test (same pattern as Plan 01: replace direct import with api.ts wrapper). However, based on the audit, these 5 modules should already be clean.
  </action>
  <verify>
    <automated>bun run test -- --run src/features/Auth/__contracts__/ src/features/Premiere/__contracts__/ src/features/Upload/__contracts__/ src/features/Settings/__contracts__/ src/features/AITools/__contracts__/</automated>
  </verify>
  <done>All 5 modules have no-bypass test sections using the comprehensive recursive pattern. All contract tests pass. Combined with Plan 01 (Baker + Trello) and BuildProject (already has it), all 8 feature modules now have no-bypass enforcement.</done>
</task>

<task type="auto">
  <name>Task 2: Update Baker requirement bookkeeping</name>
  <files>
    .planning/phases/07-baker-module/07-01-SUMMARY.md
    .planning/REQUIREMENTS.md
  </files>
  <action>
  Per CONTEXT.md locked decision: bookkeeping happens AFTER code fixes are done (Plan 01 completed the fixes).

  1. **Update SUMMARY 07-01 frontmatter:**
     - Read `.planning/phases/07-baker-module/07-01-SUMMARY.md`
     - Find `requirements_completed` field in YAML frontmatter
     - Change from `requirements_completed: []` (or whatever current value) to:
       ```yaml
       requirements_completed: [BAKR-01, BAKR-02, BAKR-03]
       ```

  2. **Update REQUIREMENTS.md:**
     - Read `.planning/REQUIREMENTS.md`
     - Check the BAKR-01, BAKR-02, BAKR-03 checkboxes:
       ```markdown
       - [x] **BAKR-01**: User can import Baker components, hooks, and types from `@features/Baker` barrel only
       - [x] **BAKR-02**: User can see an API layer wrapping Baker-related Tauri commands
       - [x] **BAKR-03**: User can see contract tests validating Baker module's public interface
       ```
     - Update the traceability table -- change BAKR-01/02/03 status from `Pending` to `Complete`
  </action>
  <verify>
    <automated>grep -c "\[x\] \*\*BAKR" .planning/REQUIREMENTS.md | grep -q "3" && grep "BAKR-01, BAKR-02, BAKR-03" .planning/phases/07-baker-module/07-01-SUMMARY.md && echo "PASS"</automated>
  </verify>
  <done>BAKR-01/02/03 checkboxes checked in REQUIREMENTS.md. Traceability table shows Complete for all three. SUMMARY 07-01 frontmatter lists BAKR-01, BAKR-02, BAKR-03 in requirements_completed.</done>
</task>

</tasks>

<verification>
- `bun run test -- --run` (full suite green)
- All 8 feature modules have "No Direct Plugin Imports" describe block in their contract tests
- REQUIREMENTS.md shows `[x]` for BAKR-01, BAKR-02, BAKR-03
- SUMMARY 07-01 frontmatter includes `requirements_completed: [BAKR-01, BAKR-02, BAKR-03]`
</verification>

<success_criteria>
- Auth, Premiere, Upload, Settings, AITools all have no-bypass contract test sections
- All no-bypass tests pass (no undiscovered bypass violations)
- BAKR-01/02/03 marked [x] in REQUIREMENTS.md
- BAKR-01/02/03 status is Complete in traceability table
- SUMMARY 07-01 requirements_completed includes all three BAKR IDs
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/10-api-bypass-fixes/10-02-SUMMARY.md`
</output>
